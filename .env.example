# Handout service. Copy to .env for local development.
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
# HANDOUT_DATA_DIR=./var/data

# HAN-6 adds the database. The workbench provides POSTGRES_URL in the environment;
# do not copy its value into this file.
# DATABASE_URL=$POSTGRES_URL
